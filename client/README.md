# Docker - React Application
This is a React js dockerized project.

- This directory contains Dockerfile, that holds docker command to build docker image for running this application using docker container.