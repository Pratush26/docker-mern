# Docker
This is a MERN stack dockerized project, that includes how to dockerized node applications and create compose.yml file.

- This root directory contains compose.yml and the individual directory(client, server) contain seperate dockerized node application.
- The docker compose file can used to hold docker command of single or multiple dockerized application to run them simentonusly.